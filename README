The ShoppingCartMVC is a light-weight demo  example of usage of the MVC(model view controller) design pattern for the shopping cart functionality. This Java application uses as a view a simple GUI which is implemented using AWT und Swing GTKs. See the class diagram (structure_diagram_eng.png) to get an idea of how this project is organized.
